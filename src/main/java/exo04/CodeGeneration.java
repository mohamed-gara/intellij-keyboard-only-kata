package exo04;

public class CodeGeneration {
    // generate constructor then add a param and affect it to a field

    public String emptyIfNull(String value) {
        // version 1: using an if
        // version 2: using an optional
        // version 3: custom postfix .ein ?:
        // version 4: use custom template ?:
        return null;
    }
}